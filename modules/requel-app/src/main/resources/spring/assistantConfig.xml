<?xml version="1.0" encoding="UTF-8"?>
<!--
  - Spring config for assistants configuration.
  -->
<beans xmlns="http://www.springframework.org/schema/beans"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xmlns:context="http://www.springframework.org/schema/context"
		xmlns:tx="http://www.springframework.org/schema/tx"
		xmlns:util="http://www.springframework.org/schema/util"
		xsi:schemaLocation="
			http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
			http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
			http://www.springframework.org/schema/tx https://www.springframework.org/schema/tx/spring-tx.xsd
			http://www.springframework.org/schema/util https://www.springframework.org/schema/util/spring-util.xsd
			">

	<!--
	  Production: async thread pool for NLP analysis tasks.
	  Tests (profile "test"): synchronous executor so analysis completes before returning,
	  preventing background threads from leaking into later tests that share the H2 database.
	-->
	<beans profile="!test">
		<bean id="taskExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
		  <property name="corePoolSize" value="5" />
		  <property name="maxPoolSize" value="10" />
		  <property name="queueCapacity" value="25" />
		</bean>
	</beans>
	<beans profile="test">
		<!-- Discard NLP analysis tasks entirely in tests.
		     SyncTaskExecutor would run them synchronously, but after the command
		     transaction commits the Hibernate session is closed, causing
		     LazyInitializationException on lazy collections in the NLP pipeline.
		     NLP behaviour is covered by the dedicated NLP test classes. -->
		<bean id="taskExecutor" class="com.rreganjr.TestNoOpTaskExecutor"/>
	</beans>

</beans>