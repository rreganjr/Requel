 docker build -t requel .

 docker tag requel rreganjr/requel:1.0c

 docker push rreganjr/requel:1.0c

 docker swarm init --advertise-addr 10.0.2.15

 docker stack deploy -c docker-compose.yml requelApp

 ------------------ new stuff --------------------------------------
 use power shell, docker-machine env will show how to setup environment so docker commands work

 cd D:\ron\IdeaProjects\requel

 docker-machine env

 & "C:\Program Files\Docker Toolbox\docker-machine.exe" env | Invoke-Expression

 docker login

 docker build -t requel .

 docker tag requel rreganjr/requel:1.0.2

 docker push rreganjr/requel:1.0.2

 -------------------------

 docker run --name requelDB -e MYSQL_ROOT_PASSWORD=hahaha -p 3306:3306 -d mysql:5.6.32

 docker run --name requel -p8080:8080 requel --spring.datasource.url=jdbc:mysql://192.168.99.100:3306/requeldb?createDatabaseIfNotExist=true --spring.datasource.password=hahaha

 from windows cmd run bash before using docker-machine ssh so keyboard mapping works

 docker-machine ssh


 svn checkout --username=rreganjr svn+ssh://rreganjr@svn.code.sf.net/p/requel/code/trunk requel-code



 -------------runing with docker-compose.yml ----------------

docker-machine restart  -- do this to have clean instances of database and web server
docker-machine env
& "C:\Program Files\Docker Toolbox\docker-machine.exe" env | Invoke-Expression
docker-compose up
